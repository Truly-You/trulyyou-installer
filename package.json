{
  "name": "trulyyou-cloudflare-installation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "node deploy.mjs"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  },
  "cloudflare": {
    "bindings": {
      "OWNER_EMAIL": {
        "description": "Your dashboard owner. Verify this email during first sign-in."
      },
      "CLOUDFLARE_ZONE_ID": {
        "description": "An active DNS zone in your account for the secure packet gateway."
      },
      "PROVISIONING_TOKEN": {
        "description": "Your Cloudflare token with Workers, D1, Containers/registry and zone DNS permissions. Stored only in your installation."
      },
      "COMPANY_NAME": {
        "description": "Your company name, used for dashboard emails."
      }
    }
  }
}
